import { createThemeFromColor } from './custom';

export const theme = createThemeFromColor('#F5F5F5', 'light-gray', 'Light Gray', {
  tintColor: '#841209',
  override: {
    primaryBackgroundColor: '#000000',
  },
});
